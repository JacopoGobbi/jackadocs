<?xml version="1.0" encoding="UTF-8"?>
<project xmlns="http://maven.apache.org/POM/4.0.0" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance"
  xsi:schemaLocation="http://maven.apache.org/POM/4.0.0 http://maven.apache.org/maven-v4_0_0.xsd">
  <modelVersion>4.0.0</modelVersion>

  <artifactId>jackadocs_2.12</artifactId>
  <version>0.1.0</version>

  <name>jackadocs</name>
  <description>Tool library for automatic generation of tool documentation.</description>
  <url>https://github.com/jackadull/jackadocs</url>

  <parent>
    <groupId>net.jackadull</groupId>
    <artifactId>scala-parent_2.12</artifactId>
    <version>1.0.0</version>
  </parent>

  <licenses>
    <license>
      <name>MIT License</name>
      <url>http://www.opensource.org/licenses/mit-license.php</url>
    </license>
  </licenses>

  <developers>
    <developer>
      <name>Matthias Deja</name>
      <email>m@jackadull.net</email>
      <organization>Jackadull</organization>
      <organizationUrl>http://jackadull.net</organizationUrl>
    </developer>
  </developers>

  <scm>
    <connection>scm:git:git://github.com/jackadull/jackadocs.git</connection>
    <developerConnection>scm:git:git://github.com/jackadull/jackadocs.git</developerConnection>
    <url>https://github.com/jackadull/jackadocs</url>
  </scm>

  <properties>
    <maven.compiler.source>${version.jvm.source}</maven.compiler.source> <!-- TODO move to parent -->
    <maven.compiler.target>${version.jvm.target}</maven.compiler.target> <!-- TODO move to parent -->
    <project.build.sourceEncoding>UTF-8</project.build.sourceEncoding> <!-- TODO move to parent -->
    <version.plugin.templating>1.0.0</version.plugin.templating>
  </properties>

  <dependencies>
    <dependency>
      <groupId>org.scala-lang.modules</groupId>
      <artifactId>scala-xml_${version.language.scala.compat}</artifactId>
      <scope>provided</scope>
    </dependency>
  </dependencies>

  <build>
    <plugins>
      <plugin> <!-- TODO define in parent pluginManagement -->
        <groupId>org.codehaus.mojo</groupId>
        <artifactId>templating-maven-plugin</artifactId>
        <version>${version.plugin.templating}</version>
        <executions>
          <execution>
            <id>filter-src</id>
            <goals>
              <goal>filter-sources</goal>
            </goals>
          </execution>
        </executions>
      </plugin>
    </plugins>
  </build>
</project>
